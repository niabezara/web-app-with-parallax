import HeroParallax from "../Parallax/HeroParallax";

const Hero: React.FC = () => {
  return (
    <div className="h-screen relative">
      <HeroParallax />
    </div>
  );
};

export default Hero;
